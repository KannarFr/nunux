[Unit]
Description=daily restic backup

[Timer]
OnCalendar=daily
RandomizedDelaySec=15m
# Run on next boot/login if the scheduled time was missed (laptop asleep/off).
Persistent=true

[Install]
WantedBy=timers.target
