---
description: Run sbt prepare, commit changes, and generate GitLab MR
tags:
  - project
---

Execute the following workflow:

1. Run `sbt prepare` command to prepare the project
2. Create a commit with all changes using `/commit`
3. Generate a GitLab merge request using `/mr`
4. Display the merge request content as markdown

Complete all steps in sequence and provide the final MR output in markdown format.
